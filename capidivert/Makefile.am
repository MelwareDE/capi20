AUTOMAKE_OPTIONS        = foreign
CLEANFILES		= *~ *%
MAINTAINERCLEANFILES    = configure aclocal.m4 Makefile.in config.h.in \
                          stamp-h.in config.guess install-sh \
                          missing mkinstalldirs config.sub
SUBDIRS 		= capidivert
